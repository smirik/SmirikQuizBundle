{% extends '::base.html.twig' %}

{% block title %}{{ 'admin.quiz.users.title'|trans }}{% endblock %}

{% block header %}
<h1>{{ 'admin.quiz.users.title'|trans }} <small>{{ 'admin.quiz.users.title.comment'|trans }}</small></h1>
{% endblock %}

{% block body %}
  <h3>{{ 'admin.quiz.users.list.title'|trans }}</h3>
  <form action="" method="get">
    <ul>
    {% for user in users %}
      <li>
        <input type="checkbox" name="user{{ user.id }}" value="1">&nbsp;&nbsp;
        {{ user.username }}
      </li>
    {% endfor %}
    </ul>
  </form>
{% endblock %}