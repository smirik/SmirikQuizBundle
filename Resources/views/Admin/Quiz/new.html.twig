<h1>Quiz creation</h1>

<form action="{{ path('smirik_quiz_admin_quiz_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_quiz') }}">
            Back to the list
        </a>
    </li>
</ul>
